export enum AUTH_CREDS {
    ACCESS_TOKEN = 'accessToken',
    REFRESH_TOKEN = 'refreshToken',
    EXPIRES_AT = 'expiresAt'
}

export enum FORM_CONSTRAINTS {
    MAX_PASSWORD_LENGTH = 8
}

export enum FORM_INPUT_IDS {
    EMAIL = "email",
    PASSWORD = "password"
}